# Retired slide decks

`standard_model_internal.tex` was an earlier internal-audience deck. It
was kept in parallel with `standard_model_external.tex` for a while but
became too much to maintain. Retired as of May 17, 2026.

The current canonical deck is `outputs/slides/standard_model.tex`
(renamed from `standard_model_external.tex`).
